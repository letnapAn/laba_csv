/**
 * Компоненты слоя DTO и маппинга данных.
 * <p>
 * Пакет содержит:
 * <ul>
 *   <li>{@link org.example.dto.EmployeeReader} — контракт для чтения сырых данных</li>
 *   <li>{@link org.example.dto.EmployeeMapper} — маппер DTO → domain entity</li>
 * </ul>
 * <p>
 * Отвечает за трансляцию данных из формата источника (CSV) в доменные объекты,
 * обеспечивая разделение ответственности между слоями приложения.
 *
 * <p> Мапперы используют кэширование для оптимизации создания связанных сущностей.
 * @since 1.0
 */
package org.example.dto;